---
phase: quick
plan: 260402-x4d
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true
requirements: [QUICK-ADD-README]

must_haves:
  truths:
    - "README.md exists at the project root"
    - "README clearly describes what the dashboard is and who it's for"
    - "README explains prerequisites (Grafana + InfluxDB) and how to import the dashboard"
    - "README covers the solar system hardware context and InfluxDB data structure"
  artifacts:
    - path: "README.md"
      provides: "Project documentation"
      contains: "## Installation"
  key_links: []
---

<objective>
Create a README.md at the project root that documents the Solar PV Monitoring Grafana Dashboard.

Purpose: New contributors (or the homeowner returning to the project) need context on what this is, what hardware it monitors, and how to import/use the dashboard.
Output: README.md with project overview, prerequisites, installation, and data schema reference.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md</name>
  <files>README.md</files>
  <action>
Create README.md at the project root covering:

1. **Title + badge row**: `# Solar PV Monitoring Dashboard` — add a short tagline ("Grafana dashboard for a residential 8-panel rooftop solar system").

2. **Screenshot placeholder**: `> _Screenshot: coming soon_` (single line, easy to update later).

3. **Overview** (2-3 sentences): What this is — Grafana JSON dashboard monitoring two micro inverters (East + West, 4 panels each), a Smart Meter, and storing data in InfluxDB 3 Core. Summarise the core value from PROJECT.md.

4. **Features** (bullet list):
   - Total & per-inverter solar production (real-time W + cumulative kWh)
   - House load (direct CT measurement, per-floor breakdown)
   - Grid import monitoring (voltage, frequency, power factor)
   - Module-level monitoring — 8 individual panels (power, voltage, current)
   - Roof layout heatmap (Canvas panel, physical panel positions)
   - Financial savings at flat 3.5 THB/kWh
   - Inverter health & state history
   - Data reconciliation: CT-measured vs calculated (grid + solar) house load

5. **Prerequisites**:
   - Grafana 12.4.1 (OSS or Enterprise)
   - InfluxDB 3 Core with SQL query support
   - A datasource named `influxdb-solar` configured in Grafana pointing to your InfluxDB instance
   - Data already flowing into InfluxDB `solar` bucket with three measurements: `East Microinverter`, `West Microinverter`, `Smart Meter`

6. **Installation / Import**:
   - Step 1: In Grafana, go to **Dashboards → Import**
   - Step 2: Upload `solar-pv-monitor.json` (or paste its contents)
   - Step 3: When prompted, map the `influxdb-solar` input to your configured datasource
   - Step 4: Click **Import**

7. **Hardware Reference** (collapsible `<details>` block):
   - System: 8 × Longi LR7-72HVDF-655M (655 W each) = 5.24 kWp
   - Two micro inverters (2250 W, 4 MPPT each): East-facing + West-facing
   - Smart Meter at grid connection (zero-export configuration)
   - CT meters on both consumer units (Floor 1 and Floor 2)
   - Panel physical layout: `East: [PV3][PV4][PV1][PV2]` / `West: [PV3][PV4][PV1][PV2]`

8. **InfluxDB Data Structure** (collapsible `<details>` block):
   - Three measurements table from PROJECT.md
   - Note: SQL query language (InfluxDB 3 Core), `time` column in nanoseconds, `device_name`/`device_type` as tag columns

9. **Dashboard Sections** (brief list):
   - Overview Stats
   - Production & Per-Inverter (East / West)
   - Module Level & Inverter Health
   - Grid Status
   - Financial Savings
   - Power Distribution (per-floor CT monitoring)
   - Backfeed Log

10. **Tech Stack** (one-liner table): Grafana 12.4.1 | InfluxDB 3 Core | SQL queries | Thai Baht (3.5 THB/kWh flat rate)

Use clean GitHub-flavoured Markdown. No emojis in headings. Keep it concise — README should be readable in under 5 minutes.
  </action>
  <verify>
    <automated>ls README.md &amp;&amp; wc -l README.md</automated>
  </verify>
  <done>README.md exists at project root with all 10 sections, renders cleanly as Markdown, and covers overview, prerequisites, installation steps, hardware reference, and data schema.</done>
</task>

</tasks>

<verification>
- `ls README.md` — file exists at project root
- `wc -l README.md` — reasonable length (50–120 lines)
- No broken Markdown syntax (unclosed code fences, malformed tables)
</verification>

<success_criteria>
A new contributor (or the homeowner) can clone this repo, read README.md, understand what the dashboard does and what hardware it targets, and successfully import `solar-pv-monitor.json` into Grafana using the documented steps.
</success_criteria>

<output>
After completion, create `.planning/quick/260402-x4d-add-readme-md/260402-x4d-SUMMARY.md`
</output>
