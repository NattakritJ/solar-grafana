---
phase: 12-add-new-microinverter-to-dashboard
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - solar-pv-monitor.json
autonomous: true
requirements:
  - MODL-01
  - MODL-04
  - HLTH-01
must_haves:
  truths:
    - "Module-level metrics include West Microinverter 2"
    - "Roof Layout canvas includes West Slope 2"
    - "Health panels created for West Microinverter 2"
  artifacts:
    - path: "solar-pv-monitor.json"
      provides: "Dashboard configuration with new health and canvas elements"
  key_links: []
---

<objective>
Create device-specific health panels for West Microinverter 2 and update module-level monitoring visualizations.

Purpose: Provide detailed insight into the new inverter's temperature, state, alarms, and per-module performance.
Output: Modified solar-pv-monitor.json with cloned/added panels and elements.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-add-new-microinverter-to-dashboard/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create West Microinverter 2 Health Panels</name>
  <files>solar-pv-monitor.json</files>
  <action>
    Locate the existing West health panels in the "Inverter Health" row (ID 500).
    Clone the following panels for "West Microinverter 2":
    - West Temperature (Gauge) -> "West 2 Temperature"
    - West State (Stat) -> "West 2 State"
    - West Alarm (Stat) -> "West 2 Alarm"
    - West Fault (Stat) -> "West 2 Fault"
    
    For each cloned panel:
    1. Assign a new unique `id`.
    2. Change the query table from `"West Microinverter"` to `"West Microinverter 2"`.
    3. Set `gridPos.y` to 93 (placing them below the existing panels).
    4. Maintain their relative `gridPos.x` ordering.
  </action>
  <verify>
    <automated>grep -q 'West 2 Temperature' solar-pv-monitor.json</automated>
  </verify>
  <done>Four new health panels are created, bound to the new table, and positioned at y=93.</done>
</task>

<task type="auto">
  <name>Task 2: Update Module-Level Detail Panels</name>
  <files>solar-pv-monitor.json</files>
  <action>
    Locate Per-panel DC metrics panels: 14 (bar gauge) and 23 (table).
    For each panel:
    Add 4 new data targets for "West 2 PV1" to "West 2 PV4".
    Query should mirror the existing PV queries but use the `"West Microinverter 2"` table and alias names to `West2 PV1`, `West2 PV2`, etc.
  </action>
  <verify>
    <automated>grep -A 50 '"id": 23' solar-pv-monitor.json | grep -q 'West2 PV1'</automated>
  </verify>
  <done>Panels 14 and 23 contain queries for the 4 new West 2 PV modules.</done>
</task>

<task type="auto">
  <name>Task 3: Update Canvas Roof Layout</name>
  <files>solar-pv-monitor.json</files>
  <action>
    Locate the Canvas Roof Layout panel (ID 45).
    First, add 4 new query targets to Panel 45 for "West 2 PV1" to "West 2 PV4", querying the "West Microinverter 2" table (clone the existing West PV queries).
    Then, clone the canvas elements for `west_pv1` through `west_pv4` (including `_bg`, `_name`, `_watts`).
    Update the cloned elements:
    1. Rename elements to `West2 PV1` etc.
    2. Bind the text elements to the new `West2 PV*` fields (added in the query list for this panel).
    3. Offset their `top` placement to 300.
    4. Add a "West Slope 2" label element at `top: 315` (cloned from existing labels).
  </action>
  <verify>
    <automated>grep -q 'West Slope 2' solar-pv-monitor.json</automated>
  </verify>
  <done>Canvas panel includes rendering elements for West 2 modules at the bottom row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard Configuration | JSON file modification for Grafana |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02 | Tampering | solar-pv-monitor.json | accept | Local offline modification of configuration only, deployed via dashboard import. |
</threat_model>

<verification>
Dashboard panels 14, 23, 45, and the health panels list West Microinverter 2 variables correctly.
</verification>

<success_criteria>
All 3 module/health/canvas elements properly include and visually map to the third microinverter metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/12-add-new-microinverter-to-dashboard/12-02-SUMMARY.md`
</output>