---
phase: 12-add-new-microinverter-to-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - solar-pv-monitor.json
autonomous: true
requirements:
  - OVER-02
  - FINC-02
  - PROD-02
must_haves:
  truths:
    - "Lifetime Free Energy includes West Microinverter 2"
    - "Today's Free Energy includes West Microinverter 2"
    - "Savings panels (Today, MTD, YTD) include West Microinverter 2"
    - "Daily Savings Trend includes West Microinverter 2 data"
  artifacts:
    - path: "solar-pv-monitor.json"
      provides: "Dashboard configuration with updated KPIs"
  key_links: []
---

<objective>
Update aggregated KPI panels and the daily savings trend chart to include West Microinverter 2 data.

Purpose: Ensure total production and savings reflect all 3 microinverters.
Output: Modified solar-pv-monitor.json with added targets and expressions.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-add-new-microinverter-to-dashboard/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add West Microinverter 2 to Overview KPIs</name>
  <files>solar-pv-monitor.json</files>
  <action>
    Locate panels: 702 (Lifetime Free Energy), 2 (Today's Free Energy), 38 (Today's Savings), 39 (Month-to-Date Savings), 40 (Year-to-Date Savings).
    For each panel:
    1. Clone the existing `West Microinverter` data target.
    2. Change the table reference in `rawSql` to `"West Microinverter 2"`.
    3. Update its `refId` to a new unique letter (e.g., "C" or "D", whatever is next available).
    4. Update the Grafana Expression (type "math") to include the new `refId` in the sum (e.g., `$A + $B + $C` or `$A + $B + $D`).
    
    CRITICAL: Do NOT touch any CT-reliant panels like 6 (Self-Consumption), 7, 9, 10, 15, 907.
  </action>
  <verify>
    <automated>grep -A 25 '"id": 702' solar-pv-monitor.json | grep -q 'West Microinverter 2'</automated>
  </verify>
  <done>Panels 702, 2, 38, 39, and 40 contain targets for West Microinverter 2 and their expressions include the new variable.</done>
</task>

<task type="auto">
  <name>Task 2: Add West Microinverter 2 to Daily Savings Trend Chart</name>
  <files>solar-pv-monitor.json</files>
  <action>
    Locate Panel 41 (Daily Savings Trend).
    Append an additional `UNION ALL` clause in the `rawSql` string for the new device:
    `UNION ALL SELECT date_trunc('day', tz(time, 'Asia/Bangkok')) AT TIME ZONE 'UTC' AS time, MAX(today_production_sensor) * 3.5 AS savings_thb FROM "West Microinverter 2" WHERE time >= $__timeFrom AND time <= $__timeTo GROUP BY 1`
  </action>
  <verify>
    <automated>grep -A 20 '"id": 41' solar-pv-monitor.json | grep -q 'West Microinverter 2'</automated>
  </verify>
  <done>Panel 41's SQL query includes a UNION ALL clause for West Microinverter 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard Configuration | JSON file modification for Grafana |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | solar-pv-monitor.json | accept | Local offline modification of configuration only, deployed via dashboard import. |
</threat_model>

<verification>
Dashboard panels 702, 2, 38, 39, 40, and 41 correctly reflect "West Microinverter 2" integration.
</verification>

<success_criteria>
All 6 overview/KPI panels accurately display data including the third microinverter.
</success_criteria>

<output>
After completion, create `.planning/phases/12-add-new-microinverter-to-dashboard/12-01-SUMMARY.md`
</output>
