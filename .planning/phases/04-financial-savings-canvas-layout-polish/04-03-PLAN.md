---
phase: 04-financial-savings-canvas-layout-polish
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: [solar-pv-monitor.json]
autonomous: false
requirements: [FINC-01, FINC-02, FINC-03, FINC-04, MODL-04, PROD-05]

must_haves:
  truths:
    - "All production panels across the entire dashboard use consistent color schemes"
    - "All power panels use correct and consistent units (W for real-time, kW for system-level, kWh for energy)"
    - "All panels have descriptive tooltips/descriptions"
    - "Dashboard visual layout is clean with no panel overlaps or gaps"
  artifacts:
    - path: "solar-pv-monitor.json"
      provides: "Polished, consistent dashboard with all Phase 4 features"
      contains: "description"
  key_links:
    - from: "All panels"
      to: "Consistent color/unit conventions"
      via: "fieldConfig.defaults.unit and thresholds across all panels"
      pattern: "unit.*watt"
---

<objective>
Audit and polish the entire dashboard for color, unit, and description consistency after all Phase 4 panels are added.

Purpose: Phase 4 Success Criteria #5 requires consistent color schemes, correct units, and descriptive tooltips across the ENTIRE dashboard (not just new panels). This plan ensures the final dashboard is cohesive.
Output: Polished solar-pv-monitor.json with consistency fixes across all 46+ panels, verified by user.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-financial-savings-canvas-layout-polish/04-CONTEXT.md
@solar-pv-monitor.json

<interfaces>
<!-- Expected state after Plans 01 and 02 -->

Panel count: ~46+ panels (37 from Phase 1-3, 7 from Plan 01, 2 from Plan 02)
Rows: 100 (Overview), 200 (Production), 300 (Module Level), 400 (Grid & Consumption), 500 (Inverter Health), 600 (Financial Savings), 700 (Event Log)

Unit conventions established:
- Power (real-time): "watt" for per-panel, "kwatt" for system-level
- Energy: "kwatth" for kWh values
- Voltage: "volt"
- Current: "amp"
- Temperature: "celsius"
- Frequency: "hertz"
- Percentage: "percentunit" (0-1 scale)
- Currency: "suffix: THB"

Color conventions:
- Production: #1a1a2e → #614a19 → #c7a035 → #73BF69 → #1a7c11
- Grid import: blue
- Temperature: green < 40°C, yellow 40-60°C, red > 60°C
- State: 0=gray, 1=yellow, 2=green, 3=red
- Financial: green for savings
- Events: backfeed=purple, alarm=orange, fault=red, state=blue
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix unit, color, description consistency across all panels</name>
  <files>solar-pv-monitor.json</files>
  <read_first>
    - solar-pv-monitor.json (FULL READ — audit every panel's fieldConfig.defaults.unit, thresholds, and description field)
    - .planning/phases/04-financial-savings-canvas-layout-polish/04-CONTEXT.md (D-65: color/unit consistency audit, D-66: spacing/row collapse defaults, D-67: sunrise/sunset annotations optional)
    - .planning/PROJECT.md (authoritative units and rate values)
  </read_first>
  <action>
**Consistency audit checklist — check and fix each category:**

**1. Unit consistency:**
Verify every panel uses the correct Grafana unit string:
- Panels showing W (real-time power): `"unit": "watt"` — check panels 1, 9, 10, 11, 22, 34, and all per-PV panels
- Panels showing kW (system-level): `"unit": "kwatt"` — check panels 1, 4, 5, 7, 9, 10, 11
- Panels showing kWh (energy): `"unit": "kwatth"` — check panels 2, 3, 15, 41, 43
- Panels showing V: `"unit": "volt"` — check panel 16
- Panels showing Hz: `"unit": "hertz"` — check panel 17
- Panels showing A: `"unit": "amp"` — check panel 19
- Panels showing %: `"unit": "percentunit"` — check panel 6, 18
- Panels showing °C: `"unit": "celsius"` — check panels 24, 25
- Panels showing THB: `"unit": "suffix: THB"` — check panels 38-40, 42, 44
- Fix any mismatches

**2. Description/tooltip consistency:**
Every panel should have a non-empty `"description"` field explaining what it shows. Check all panels 1-46. Add descriptions where missing. Keep descriptions brief (1 sentence). Examples:
- Panel 1 (Solar Power): "Current total solar production from both inverters"
- Panel 12 (Solar Production): "Time-series graph of total solar production power"
- etc.

**3. Threshold color consistency:**
Verify production-related panels all use the same color scale:
- Production thresholds (panels showing W/kW production): dark-gray at 0, yellow at low, green at moderate, dark-green at high
- Check panels 1, 7, 9, 12, 13, 22, and the new Canvas (45)
- Financial thresholds: green tones for savings
- Temperature thresholds: green < 40, yellow 40-60, red > 60

**4. Layout/spacing fixes (per D-66):**
- Verify no panel gridPos overlaps (two panels sharing same x,y space)
- Verify row collapse defaults: All rows uncollapsed (600 was uncollapsed in Plan 01)
- Verify panel spacing is reasonable (no large gaps between sections)
- Fix any gridPos inconsistencies introduced by Plans 01 and 02

**5. Time series panel consistency:**
- Verify all time-series panels (12, 20, 21, 32) use `$__timeFrom` and `$__timeTo` for time range
- Verify line widths, fill opacity, and gradient settings are consistent

**6. Table panel consistency:**
- Verify table panels (23, 35, 36, 37) have consistent column widths and color overrides

**7. Stat panel style consistency:**
- Summary stats (1-8) in overview: transparent: true, graphMode varies
- Power flow stats (9-11): colorMode: "background", graphMode: "area"
- Financial stats (38-44): graphMode: "none", colorMode: "value"
- Verify no stat panel accidentally uses different graphMode or colorMode than its section peers

**8. Optional polish (per D-67 — only if straightforward):**
- If sunrise/sunset annotations are trivial (a few lines of annotation config), add them. If complex, skip.
- They would use InfluxDB queries or static time config for Bangkok sunrise (~06:00) and sunset (~18:00) as annotation bands.
- SKIP if this adds more than ~20 lines of JSON. This is explicitly optional.

Apply all fixes to `solar-pv-monitor.json` in a single pass. Do NOT break existing panel functionality — only adjust fieldConfig, descriptions, and gridPos.
  </action>
  <verify>
    <automated>node -e "const d=require('./solar-pv-monitor.json'); let issues=[]; function checkPanels(panels){for(const p of panels){if(!p.description&&p.type!=='row')issues.push('No desc: id='+p.id+' '+p.title);if(p.panels)checkPanels(p.panels);}} checkPanels(d.panels); console.log('Panels without description:', issues.length); if(issues.length>0)issues.slice(0,5).forEach(i=>console.log(' ',i)); const total=d.panels.filter(p=>p.type!=='row').length; console.log('Total non-row panels:', total); console.log('Valid JSON: true');"</automated>
  </verify>
  <acceptance_criteria>
    - All non-row panels have a non-empty `description` field
    - All power panels use "watt" or "kwatt" unit (not mixed with "W" strings or missing unit)
    - All energy panels use "kwatth" unit
    - All financial panels use "suffix: THB" unit
    - No two panels have overlapping gridPos (same x,y,w,h area)
    - All rows have `collapsed: false` (including Row 600 Financial Savings)
    - Production color thresholds are consistent across panels 1, 7, 9, 13, 22, 45
    - Dashboard JSON is valid and parseable
  </acceptance_criteria>
  <done>All panels across the dashboard have consistent units, colors, descriptions, and layout with no overlaps</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete dashboard in Grafana</name>
  <files>solar-pv-monitor.json</files>
  <action>
Human verification checkpoint — no automated changes. User imports the final dashboard JSON into Grafana and visually confirms all Phase 4 features work correctly with live data.
  </action>
  <how-to-verify>
1. Import `solar-pv-monitor.json` into Grafana 12.4.1 via Dashboards → Import
2. Select `influxdb-solar` as the datasource when prompted
3. Verify the following sections:

**Financial Savings (Row 600):**
- [ ] Row is expanded (not collapsed)
- [ ] 3 summary stat panels visible: "Today's Savings", "Month Savings", "Year Savings" — all showing values in THB
- [ ] 4 breakdown stat panels visible: "Peak kWh", "Peak THB", "Off-Peak kWh", "Off-Peak THB"
- [ ] Values look reasonable (Today's savings should be non-zero during daytime)

**Canvas Roof Layout (under Module Level):**
- [ ] Canvas panel shows 8 colored rectangles in 2 rows
- [ ] West row on top, East row on bottom
- [ ] West row order (left to right): PV2, PV1, PV4, PV3
- [ ] East row order (left to right): PV3, PV4, PV1, PV2
- [ ] Colors reflect current production (green during day, dark at night)
- [ ] Panel names and power values visible on each rectangle

**Daily Production Heatmap (under Production):**
- [ ] Heatmap visible showing daily patterns
- [ ] Color intensity corresponds to production level
- [ ] Time range shows data (select "Last 90 days" if needed)

**Overall Polish:**
- [ ] No panels showing "No Data" errors (except financial during night if no production)
- [ ] Consistent units across all panels
- [ ] No layout gaps or overlapping panels
- [ ] All sections visible and organized
  </how-to-verify>
  <verify>
    <automated>node -e "const d=require('./solar-pv-monitor.json'); [38,39,40,41,42,43,44,45,46].forEach(id=>{const p=d.panels.find(p=>p.id===id); console.log(id, p?'✓ '+p.title:'✗ MISSING')}); console.log('Valid JSON: true');"</automated>
  </verify>
  <done>User has imported the dashboard into Grafana 12.4.1, verified all Phase 4 features work with live data, and approved the result</done>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Dashboard JSON is valid: `node -e "require('./solar-pv-monitor.json'); console.log('Valid JSON')"`
2. Total panel count: `node -e "const d=require('./solar-pv-monitor.json'); console.log('Panels:', d.panels.filter(p=>p.type!=='row').length)"`
3. All Phase 4 panels exist: `node -e "const d=require('./solar-pv-monitor.json'); [38,39,40,41,42,43,44,45,46].forEach(id=>{const p=d.panels.find(p=>p.id===id); console.log(id, p?'✓ '+p.title:'✗ MISSING')})"`
4. Human verification confirms visual correctness in Grafana
</verification>

<success_criteria>
- All Phase 4 panels (38-46) functional with live data
- Financial savings showing correct TOU-classified values in THB
- Canvas roof layout displaying color-coded production in correct physical arrangement
- Daily production heatmap showing patterns over time
- Dashboard-wide consistency in units, colors, and descriptions
- User approves the final dashboard via visual verification in Grafana
</success_criteria>

<output>
After completion, create `.planning/phases/04-financial-savings-canvas-layout-polish/04-03-SUMMARY.md`
</output>
