---
phase: 05.1-fix-today-boundary
verified: 2026-04-01T14:30:00Z
status: passed
score: 5/5 must-haves verified
re_verification: false
---

# Phase 05.1: Fix "Today" SQL Boundary — Verification Report

**Phase Goal:** Fix all 18 SQL queries that use `now() - INTERVAL '24 hours'` / `now() - INTERVAL '1 day'` as a "today" boundary — replace with `date_trunc('day', now() AT TIME ZONE 'Asia/Bangkok') AT TIME ZONE 'UTC'` so "today" means the Bangkok calendar day, not a rolling 24-hour window.  
**Verified:** 2026-04-01T14:30:00Z  
**Status:** ✓ PASSED  
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Old rolling-window pattern `now() - INTERVAL '24 hours'` is fully eliminated | ✓ VERIFIED | `rg -c` returns 0; confirmed by commit diff (14→0) |
| 2 | Old rolling-window pattern `now() - INTERVAL '1 day'` is fully eliminated | ✓ VERIFIED | `rg -c` returns 0; confirmed by commit diff (4→0) |
| 3 | New calendar-day expression appears exactly 18 times across all affected rawSql fields | ✓ VERIFIED | `rg -c` returns exactly 18; line numbers match all 10 panel groups |
| 4 | Real-time and rolling-window patterns NOT intended for today-scope are untouched (`1 minute`, `1 hour`, month/year `date_trunc`) | ✓ VERIFIED | `1 minute` still 1 occurrence; `1 hour` still 2 occurrences; month/year `date_trunc` patterns present |
| 5 | Dashboard JSON remains structurally valid | ✓ VERIFIED | `python3 -c "json.load(...)"` → "JSON valid"; no parse errors |

**Score:** 5/5 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `solar-pv-monitor.json` | Modified with 18 calendar-day replacements | ✓ VERIFIED | 5,451 lines; 18 occurrences of new expression confirmed at lines 206, 216, 582, 592, 1508, 1517, 4432, 4442, 4717, 4727, 4812, 4822, 4907, 4917, 5002, 5012, 5114, 5191 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| Panel 2 rawSql (Today's Energy) | `date_trunc('day'...)` boundary | Lines 206, 216 | ✓ WIRED | Both East and West queries confirmed |
| Panel 7 rawSql (Peak Today) | `date_trunc('day'...)` boundary | Lines 582, 592 | ✓ WIRED | `MAX(power_sensor)` queries confirmed |
| Per-inverter stats rawSql | `date_trunc('day'...)` boundary | Lines 1508, 1517 | ✓ WIRED | East/West today production queries confirmed |
| Panel 38 rawSql (Today's Savings TOU) | `date_trunc('day'...)` boundary | Lines 4432, 4442 | ✓ WIRED | Inner subquery WHERE clause confirmed |
| Panel 41 rawSql (Peak kWh TOU) | `date_trunc('day'...)` boundary | Lines 4717, 4727 | ✓ WIRED | Inner subquery confirmed |
| Panel 42 rawSql (Peak THB TOU) | `date_trunc('day'...)` boundary | Lines 4812, 4822 | ✓ WIRED | Inner subquery confirmed |
| Panel 43 rawSql (Off-Peak kWh TOU) | `date_trunc('day'...)` boundary | Lines 4907, 4917 | ✓ WIRED | Inner subquery confirmed |
| Panel 44 rawSql (Off-Peak THB TOU) | `date_trunc('day'...)` boundary | Lines 5002, 5012 | ✓ WIRED | Inner subquery confirmed |
| Panel 33 rawSql (Backfeed Count) | `date_trunc('day'...)` boundary | Line 5114 | ✓ WIRED | Smart Meter query; `title: "Today's Backfeed Count"` confirmed |
| Panel 34 rawSql (Max Backfeed) | `date_trunc('day'...)` boundary | Line 5191 | ✓ WIRED | Smart Meter `ABS(MIN(power_sensor))` query confirmed |

---

### Data-Flow Trace (Level 4)

Not applicable. This phase applies no structural changes — it is a pure SQL string replacement within existing, already-wired `rawSql` fields. The data source (InfluxDB `solar` database) and Grafana panel bindings are unchanged. The fix affects the time boundary predicate only; data flow was already live prior to this phase.

---

### Behavioral Spot-Checks

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| No old rolling pattern remains in file | `rg -c "now() - INTERVAL '24 hours'"` → 0 | 0 (exit 1 = no matches) | ✓ PASS |
| No `1 day` pattern remains in file | `rg -c "now() - INTERVAL '1 day'"` → 0 | 0 (exit 1 = no matches) | ✓ PASS |
| New expression count = 18 | `rg -c "date_trunc('day'...)"` → 18 | 18 | ✓ PASS |
| Real-time pattern untouched | `rg -c "now() - INTERVAL '1 minute'"` → 1 | 1 | ✓ PASS |
| JSON remains valid | `python3 json.load(...)` | "JSON valid" | ✓ PASS |
| Spot-check line 206 (Today's Energy East) | Contains correct expression | `date_trunc('day', now() AT TIME ZONE 'Asia/Bangkok') AT TIME ZONE 'UTC'` | ✓ PASS |
| Spot-check line 4432 (TOU Savings East inner subquery) | Contains correct expression in inner WHERE | Confirmed in rawSql | ✓ PASS |
| Spot-check line 5114 (Backfeed Count) | Contains correct expression | Confirmed; `title: "Today's Backfeed Count"` | ✓ PASS |

---

### Requirements Coverage

| Requirement | Description | Source Plan | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| OVER-02 | User can see today's total energy production (kWh) as a stat | 05.1-01-PLAN.md | ✓ SATISFIED | Lines 206, 216: `today_kwh` queries now anchored to Bangkok 00:00 via `date_trunc` |
| OVER-09 | User can see peak power achieved today (kW) as a stat | 05.1-01-PLAN.md | ✓ SATISFIED | Lines 582, 592: `MAX(power_sensor)` queries now anchored to Bangkok 00:00 |
| PROD-02 | User can see per-inverter production breakdown (East vs West) as separate stats | 05.1-01-PLAN.md | ✓ SATISFIED | Lines 1508, 1517: East/West today production queries corrected |
| FINC-01 | User can see today's estimated savings in THB based on solar production | 05.1-01-PLAN.md | ✓ SATISFIED | Lines 4432, 4442: TOU savings inner subquery anchored to Bangkok 00:00 |
| FINC-02 | User can see TOU-aware savings using peak (5.7982 THB/kWh) and off-peak (2.6369 THB/kWh) rates | 05.1-01-PLAN.md | ✓ SATISFIED | Lines 4717–5012: All TOU breakdown queries (peak kWh, peak THB, off-peak kWh, off-peak THB) corrected |
| EVNT-02 | User can see today's backfeed event count and max backfeed power (W) as summary stats | 05.1-01-PLAN.md | ✓ SATISFIED | Lines 5114, 5191: Backfeed Count ("Today's Backfeed Count") and Max Backfeed Power queries corrected |

**Note on traceability:** REQUIREMENTS.md maps OVER-02, OVER-09, PROD-02, FINC-01, FINC-02, and EVNT-02 to Phases 1–3 (original implementation). Phase 05.1 is a bug-fix phase that corrects the time boundary semantics for these same requirements. The traceability table does not need to be updated since the requirements themselves were satisfied in prior phases; this phase improves accuracy of the existing implementation.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | — | — | — |

No anti-patterns found. The replacement is exact and consistent. No TODO/FIXME, no placeholder strings, no empty implementations, no hardcoded empty data. The `now() - INTERVAL '1 hour'` occurrences at lines 297/307 (lifetime kWh queries) were intentionally left untouched — they are not "today" scope queries and the plan explicitly excluded them.

---

### Human Verification Required

### 1. Query Semantics at Midnight Boundary

**Test:** On 2026-04-01 at 23:59 Bangkok time, note today's energy value. Wait until 00:00:01 (day rollover). Reload the dashboard.  
**Expected:** The "Today's Energy" stat resets to ~0 kWh (only seconds of data since midnight) rather than continuing to show 24h of data.  
**Why human:** Cannot be verified without live InfluxDB data at the exact day boundary.

### 2. UTC Offset Correctness

**Test:** Confirm that the boundary is 17:00 UTC (= 00:00 Bangkok, UTC+7). Check InfluxDB directly: query `WHERE time >= date_trunc('day', now() AT TIME ZONE 'Asia/Bangkok') AT TIME ZONE 'UTC'` and confirm the returned timestamp floor is always 17:00 UTC of the previous calendar day.  
**Expected:** Floor timestamp = `YYYY-MM-DDT17:00:00Z`  
**Why human:** Cannot invoke InfluxDB 3 Core without live connection.

---

### Gaps Summary

No gaps. All 18 rolling-window occurrences replaced. JSON valid. All 6 requirement IDs satisfied by confirmed rawSql content. All untouched patterns verified present. Commit `6e3db2f` exists and modifies only `solar-pv-monitor.json`.

The only open items are two human-only spot-checks that require a live InfluxDB connection at specific times — these are observational validations, not implementation gaps.

---

_Verified: 2026-04-01T14:30:00Z_  
_Verifier: the agent (gsd-verifier)_
