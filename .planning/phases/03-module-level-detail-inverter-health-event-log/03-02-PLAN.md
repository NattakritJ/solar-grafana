---
phase: 03-module-level-detail-inverter-health-event-log
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - solar-pv-monitor.json
autonomous: true
requirements:
  - HLTH-01
  - HLTH-02
  - HLTH-03
  - HLTH-04

must_haves:
  truths:
    - "User can see temperature gauges for both East and West inverters with heat-severity thresholds"
    - "User can see device state, alarm, and fault status for both inverters with color-coded indicators"
    - "User can see a state-timeline showing producing/standby/offline transitions over time"
    - "Nighttime offline state appears as gray/neutral, not red/alarming"
  artifacts:
    - path: "solar-pv-monitor.json"
      provides: "Inverter health panels in Row 500"
      contains: "Inverter Health"
  key_links:
    - from: "solar-pv-monitor.json gauge panels"
      to: "InfluxDB temperature_sensor field"
      via: "SQL SELECT on East/West Microinverter"
      pattern: "temperature_sensor"
    - from: "solar-pv-monitor.json stat panels"
      to: "InfluxDB device_state/alarm/fault fields"
      via: "SQL SELECT with value mappings"
      pattern: "device_state_sensor|alarm_sensor|fault_sensor"
---

<objective>
Add inverter health monitoring panels to the "Inverter Health" row (id=500) — temperature gauges, device state/alarm/fault status indicators, and a state-timeline panel showing operational history.

Purpose: Enable the homeowner to monitor inverter health and quickly spot overheating or fault conditions, while treating nighttime offline as normal.
Output: Updated `solar-pv-monitor.json` with ~9 new panels (2 gauges, 6 stats, 1 state-timeline) in the uncollapsed Inverter Health row.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-module-level-detail-inverter-health-event-log/03-CONTEXT.md
@.planning/phases/03-module-level-detail-inverter-health-event-log/03-01-SUMMARY.md

<interfaces>
<!-- From prior plan output and existing dashboard -->

From solar-pv-monitor.json (after Plan 01):
- Last panel id: 23 (from Plan 01). New panels start at id=24.
- Row 500 "Inverter Health": currently collapsed=true, empty panels=[]
- Panel 8 "System Status" provides value mapping template for device_state_sensor

From InfluxDB schema (PROJECT.md):
- Health fields on each inverter measurement:
  - `temperature_sensor` (Float64, degrees Celsius)
  - `device_state_sensor` (Float64, enum: 0=Standby, 1=Normal/Producing, 2=Warning, 3=Fault)
  - `alarm_sensor` (Float64, 0=no alarm, non-zero=active alarm code)
  - `fault_sensor` (Float64, 0=no fault, non-zero=active fault code)
- Measurements: "East Microinverter", "West Microinverter"

Key decisions from CONTEXT.md:
- D-29: Nighttime shows 0W with dark-gray, NOT "No Data"
- D-30: Offline/sleeping = gray (not red). HLTH-04 requirement.
- D-31: Temperature at night shows last known value
- D-32: Gauge panels for temperature, thresholds 0-40 green, 40-60 yellow, 60+ red
- D-33: 3 stats per inverter (state, alarm, fault) = 6 total
- D-34: Device state mapping: 0="Offline", 1="Standby", 2="Producing" (tentative — verify from data)
- D-35: Alarm/fault: 0=green, non-zero=red
- D-37: State-timeline for operational history
- D-45: Panels go under "Inverter Health" row (id=500)
- D-49: Temperature: green->yellow->red gradient
- D-50: State: green=producing, yellow=standby, gray=offline

Established patterns from Phase 1:
- Value mappings JSON structure (see Panel 8 System Status)
- gauge panel type needs to be added to __requires
- state-timeline panel type needs to be added to __requires
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Uncollapse Inverter Health row and add temperature gauges + status stats</name>
  <files>solar-pv-monitor.json</files>
  <read_first>
    - solar-pv-monitor.json (find row id=500 position after Plan 01, verify panel 23 is last)
    - .planning/phases/03-module-level-detail-inverter-health-event-log/03-CONTEXT.md (D-30 through D-35, D-45, D-49, D-50)
  </read_first>
  <action>
**Step 1: Uncollapse Row 500 "Inverter Health"**
- Set row id=500 `collapsed: false`
- Panels inside this row start at the y position after the Module Level section ends
- Move rows 600, 700 down to accommodate

**Step 2: Add Panel id=24 — East Inverter Temperature Gauge (HLTH-01 per D-32)**
- Type: `gauge`
- id: 24
- Title: "East Temperature"
- Description: "East Microinverter operating temperature"
- gridPos: `{ h: 6, w: 4, x: 0, y: <after row 500> }` (compact gauge)
- Query (refId: "A"):
  ```sql
  SELECT COALESCE(temperature_sensor, 0) AS "Temperature" FROM "East Microinverter" ORDER BY time DESC LIMIT 1
  ```
- Format: `table`
- fieldConfig.defaults:
  - unit: `"celsius"`
  - min: 0
  - max: 80
  - thresholds (per D-32/D-49):
    ```json
    { "mode": "absolute", "steps": [
      { "value": null, "color": "green" },
      { "value": 40, "color": "yellow" },
      { "value": 60, "color": "red" }
    ]}
    ```
  - color: `{ "mode": "thresholds" }`
- options:
  ```json
  {
    "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
    "showThresholdLabels": false,
    "showThresholdMarkers": true,
    "sizing": "auto"
  }
  ```

**Step 3: Add Panel id=25 — West Inverter Temperature Gauge (HLTH-01 per D-32)**
- Same as Panel 24 but:
  - id: 25
  - Title: "West Temperature"
  - Description: "West Microinverter operating temperature"
  - gridPos: `{ h: 6, w: 4, x: 4, y: <same as 24> }`
  - Query: `SELECT COALESCE(temperature_sensor, 0) AS "Temperature" FROM "West Microinverter" ORDER BY time DESC LIMIT 1`

**Step 4: Add 6 Status Stat Panels (HLTH-02 per D-33) — 3 per inverter**

**Panel id=26 — East Device State**
- Type: `stat`
- gridPos: `{ h: 3, w: 4, x: 8, y: <same as 24> }`
- Title: "East State"
- Query: `SELECT device_state_sensor AS state FROM "East Microinverter" ORDER BY time DESC LIMIT 1`
- Format: `table`
- Value mappings (per D-34, D-30 nighttime=gray per HLTH-04):
  ```json
  [
    {
      "type": "value",
      "options": {
        "0": { "text": "Offline", "color": "dark-gray" },
        "1": { "text": "Standby", "color": "#EAB839" },
        "2": { "text": "Producing", "color": "green" },
        "3": { "text": "Fault", "color": "red" }
      }
    },
    {
      "type": "special",
      "options": {
        "match": "null",
        "result": { "text": "Offline", "color": "dark-gray" }
      }
    }
  ]
  ```
- options: `{ "reduceOptions": { "calcs": ["lastNotNull"] }, "graphMode": "none", "textMode": "value", "colorMode": "background" }`

**Panel id=27 — East Alarm**
- Type: `stat`
- gridPos: `{ h: 3, w: 4, x: 12, y: <same as 24> }`
- Title: "East Alarm"
- Query: `SELECT COALESCE(alarm_sensor, 0) AS alarm FROM "East Microinverter" ORDER BY time DESC LIMIT 1`
- Value mappings (per D-35):
  ```json
  [
    {
      "type": "value",
      "options": {
        "0": { "text": "Normal", "color": "green" }
      }
    },
    {
      "type": "special",
      "options": {
        "match": "null",
        "result": { "text": "N/A", "color": "dark-gray" }
      }
    },
    {
      "type": "range",
      "options": {
        "from": 1,
        "to": 99999,
        "result": { "text": "ALARM", "color": "red" }
      }
    }
  ]
  ```
  Note: The range mapping catches any non-zero alarm code and shows "ALARM" in red. The raw code is also visible via the `textMode: "value_and_name"` — actually, per D-35 "display raw code alongside color", use `textMode: "auto"` and the value mapping text to show ALARM + code. Actually simpler: use `textMode: "value"` with value=0 mapped to "Normal" and non-zero range mapped to text that includes the code. Since value mappings replace the display, for non-zero we can't show the code via mappings alone. Use `textMode: "value_and_name"` so Grafana shows both the mapped text and the raw value.
  
  Revised: Set `textMode: "value"` and for the range mapping, use `text: "ALARM ${__value.raw}"`. Actually Grafana doesn't template inside mappings. Simplest approach: don't map non-zero — let the raw alarm code display, and use thresholds for coloring:
  - thresholds: `[{ value: null, color: "green" }, { value: 1, color: "red" }]`
  - Only map 0 to "Normal" text, null to "N/A". Non-zero values show as their raw code number in red.

  Revised value mappings:
  ```json
  [
    {
      "type": "value",
      "options": {
        "0": { "text": "Normal", "color": "green" }
      }
    },
    {
      "type": "special",
      "options": {
        "match": "null",
        "result": { "text": "N/A", "color": "dark-gray" }
      }
    }
  ]
  ```
  With thresholds: `{ "mode": "absolute", "steps": [{ "value": null, "color": "green" }, { "value": 1, "color": "red" }] }`
  This way: 0 shows "Normal" (green), null shows "N/A" (gray), any non-zero shows the raw code number in red.
- options: `{ "reduceOptions": { "calcs": ["lastNotNull"] }, "graphMode": "none", "textMode": "value", "colorMode": "background" }`

**Panel id=28 — East Fault**
- Same pattern as Panel 27 but:
  - id: 28
  - Title: "East Fault"
  - Query: `SELECT COALESCE(fault_sensor, 0) AS fault FROM "East Microinverter" ORDER BY time DESC LIMIT 1`
  - gridPos: `{ h: 3, w: 4, x: 16, y: <same as 24> }`
  - Same value mappings and thresholds as alarm (0=Normal/green, non-zero=code/red, null=N/A/gray)

**Panel id=29 — West Device State**
- Same as Panel 26 but for West:
  - id: 29
  - gridPos: `{ h: 3, w: 4, x: 8, y: <24.y + 3> }` (below East status row)
  - Query: `SELECT device_state_sensor AS state FROM "West Microinverter" ORDER BY time DESC LIMIT 1`

**Panel id=30 — West Alarm**
- Same as Panel 27 but for West:
  - id: 30
  - gridPos: `{ h: 3, w: 4, x: 12, y: <24.y + 3> }`
  - Query: `SELECT COALESCE(alarm_sensor, 0) AS alarm FROM "West Microinverter" ORDER BY time DESC LIMIT 1`

**Panel id=31 — West Fault**
- Same as Panel 28 but for West:
  - id: 31
  - gridPos: `{ h: 3, w: 4, x: 16, y: <24.y + 3> }`
  - Query: `SELECT COALESCE(fault_sensor, 0) AS fault FROM "West Microinverter" ORDER BY time DESC LIMIT 1`

**Step 5: Add `gauge` and `state-timeline` panel types to `__requires` array:**
```json
{ "type": "panel", "id": "gauge", "name": "Gauge", "version": "" },
{ "type": "panel", "id": "state-timeline", "name": "State timeline", "version": "" }
```

**Step 6: Adjust gridPos of downstream rows (600, 700)**

**CRITICAL for HLTH-04:** All nighttime/offline states must use gray coloring, NOT red. The value mappings above explicitly map state=0 and null to "Offline" with `dark-gray` color. This is the single most important UX requirement — nighttime shutdown is expected and normal.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('solar-pv-monitor.json') as f:
    d = json.load(f)
# Check row 500 uncollapsed
r500 = [p for p in d['panels'] if p.get('id') == 500][0]
assert r500['collapsed'] == False, 'Row 500 should be uncollapsed'
# Check gauge panels exist
p24 = [p for p in d['panels'] if p.get('id') == 24][0]
p25 = [p for p in d['panels'] if p.get('id') == 25][0]
assert p24['type'] == 'gauge', 'Panel 24 should be gauge'
assert p25['type'] == 'gauge', 'Panel 25 should be gauge'
assert 'temperature_sensor' in p24['targets'][0]['rawSql']
# Check 6 stat panels
for pid in [26, 27, 28, 29, 30, 31]:
    p = [p for p in d['panels'] if p.get('id') == pid][0]
    assert p['type'] == 'stat', f'Panel {pid} should be stat'
# Check HLTH-04: offline state mapped to gray, not red
p26 = [p for p in d['panels'] if p.get('id') == 26][0]
mappings = p26['fieldConfig']['defaults']['mappings']
has_gray_offline = False
for m in mappings:
    if m.get('type') == 'value' and '0' in m.get('options', {}):
        if 'gray' in m['options']['0'].get('color', '').lower() or m['options']['0'].get('color') == 'dark-gray':
            has_gray_offline = True
    if m.get('type') == 'special' and m.get('options', {}).get('match') == 'null':
        if 'gray' in m['options']['result'].get('color', '').lower() or m['options']['result'].get('color') == 'dark-gray':
            has_gray_offline = True
assert has_gray_offline, 'HLTH-04: Offline state must be gray, not red'
# Check __requires
req_ids = [r['id'] for r in d['__requires']]
assert 'gauge' in req_ids, 'gauge must be in __requires'
assert 'state-timeline' in req_ids, 'state-timeline must be in __requires'
print('PASS: Task 1 verified')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Row id=500 has `collapsed: false`
    - Panels 24-25 are `gauge` type with temperature_sensor queries and green/yellow/red thresholds at 40/60
    - Panels 26-31 are `stat` type for device_state, alarm, fault (3 per inverter)
    - Panel 26 and 29 (device state) have value mappings: 0="Offline"/dark-gray, 1="Standby"/yellow, 2="Producing"/green, 3="Fault"/red, null="Offline"/dark-gray (HLTH-04 compliance)
    - Panels 27-28, 30-31 (alarm/fault) map 0="Normal"/green, null="N/A"/dark-gray, threshold >=1 = red
    - `__requires` includes gauge and state-timeline panel types
    - JSON parses without errors
  </acceptance_criteria>
  <done>Inverter Health row shows temperature gauges and status indicators for both inverters. Nighttime offline is gray/neutral per HLTH-04.</done>
</task>

<task type="auto">
  <name>Task 2: Add device state timeline panel for operational history</name>
  <files>solar-pv-monitor.json</files>
  <read_first>
    - solar-pv-monitor.json (verify panels 24-31 exist from Task 1, find correct y position)
    - .planning/phases/03-module-level-detail-inverter-health-event-log/03-CONTEXT.md (D-37, D-50)
  </read_first>
  <action>
**Add Panel id=32 — Device State Timeline (HLTH-02 enhancement per D-37)**
- Type: `state-timeline`
- id: 32
- Title: "Inverter State History"
- Description: "Operating state transitions over time — producing, standby, offline"
- gridPos: `{ h: 6, w: 24, x: 0, y: <below status stats> }` (full width)
- Datasource: `{ "type": "influxdb", "uid": "${DS_INFLUXDB_SOLAR}" }`

**Queries (2 — one per inverter, for two timeline lanes):**

Query A (refId: "East"):
```sql
SELECT time, device_state_sensor AS "East Inverter"
FROM "East Microinverter"
WHERE time >= $__timeFrom AND time <= $__timeTo
ORDER BY time
```

Query B (refId: "West"):
```sql
SELECT time, device_state_sensor AS "West Inverter"
FROM "West Microinverter"
WHERE time >= $__timeFrom AND time <= $__timeTo
ORDER BY time
```

- Format: `time_series`
- fieldConfig.defaults:
  - color: `{ "mode": "thresholds" }`
  - thresholds (matching D-50 state colors):
    ```json
    { "mode": "absolute", "steps": [
      { "value": null, "color": "dark-gray" },
      { "value": 0, "color": "dark-gray" },
      { "value": 1, "color": "#EAB839" },
      { "value": 2, "color": "green" },
      { "value": 3, "color": "red" }
    ]}
    ```
  - mappings (per D-50):
    ```json
    [
      {
        "type": "value",
        "options": {
          "0": { "text": "Offline", "color": "dark-gray" },
          "1": { "text": "Standby", "color": "#EAB839" },
          "2": { "text": "Producing", "color": "green" },
          "3": { "text": "Fault", "color": "red" }
        }
      },
      {
        "type": "special",
        "options": {
          "match": "null",
          "result": { "text": "No Data", "color": "dark-gray" }
        }
      }
    ]
    ```
- options:
  ```json
  {
    "showValue": "auto",
    "rowHeight": 0.9,
    "mergeValues": true,
    "alignValue": "left",
    "legend": { "displayMode": "list", "placement": "bottom" },
    "tooltip": { "mode": "single" }
  }
  ```

**Adjust downstream rows (600, 700) y positions to accommodate this panel.**
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('solar-pv-monitor.json') as f:
    d = json.load(f)
p32 = [p for p in d['panels'] if p.get('id') == 32][0]
assert p32['type'] == 'state-timeline', 'Panel 32 should be state-timeline'
assert len(p32['targets']) == 2, 'Panel 32 should have 2 queries (East + West)'
assert 'device_state_sensor' in p32['targets'][0]['rawSql']
assert '__timeFrom' in p32['targets'][0]['rawSql'], 'State timeline must use time range'
# Check value mappings include gray for offline
mappings = p32['fieldConfig']['defaults'].get('mappings', [])
assert len(mappings) >= 1, 'Must have value mappings for state codes'
print('PASS: Task 2 verified')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Panel id=32 exists with type `state-timeline`, title "Inverter State History"
    - Has 2 queries: one for "East Inverter" and one for "West Inverter"
    - Both queries use `$__timeFrom` and `$__timeTo` for time range awareness
    - Value mappings: 0=Offline/gray, 1=Standby/yellow, 2=Producing/green, 3=Fault/red
    - `mergeValues: true` in options to consolidate adjacent same-state periods
    - JSON parses without errors
  </acceptance_criteria>
  <done>State timeline shows two lanes (East/West) with color-coded operational history. Offline periods are gray, producing is green.</done>
</task>

</tasks>

<verification>
- `python3 -c "import json; d=json.load(open('solar-pv-monitor.json')); assert len([p for p in d['panels'] if p.get('id') in range(24,33)]) == 9"` — All 9 new panels exist
- Row 500 is uncollapsed
- All queries use valid InfluxDB 3 SQL syntax
- HLTH-04 verified: device state 0 and null map to gray in all state-related panels
- Temperature thresholds: green < 40, yellow 40-60, red > 60
- `__requires` includes gauge and state-timeline
</verification>

<success_criteria>
User can see:
1. Temperature gauges for both inverters with green/yellow/red thresholds (HLTH-01)
2. Device state, alarm, and fault indicators for both inverters with appropriate colors (HLTH-02)
3. State timeline showing when inverters were producing/standby/offline (HLTH-02, HLTH-03 partial)
4. Nighttime offline displayed as neutral gray, not alarming red (HLTH-04)
</success_criteria>

<output>
After completion, create `.planning/phases/03-module-level-detail-inverter-health-event-log/03-02-SUMMARY.md`
</output>
